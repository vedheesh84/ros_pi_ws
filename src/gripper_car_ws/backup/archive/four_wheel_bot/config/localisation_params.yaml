slam_toolbox:
  ros__parameters:

    # ============================================================
    # Solver Plugin Settings
    # ============================================================
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ============================================================
    # Core TF / Topic Configuration
    # ============================================================
    odom_frame: odom
    map_frame: map
    base_frame: body_link
    scan_topic: /scan
    use_map_saver: true
    odom_topic: /odom

    # Mode: "mapping" = build map, "localization" = use map
    mode: localization
    enable_interactive_mode: false

    # if you'd like to immediately start continuing a map at a given pose
    # or at the dock, but they are mutually exclusive, if pose is given
    # will use pose
    map_file_name: /home/ved/ros2_doc/gripper_car_ws/src/four_wheel_bot/map/my_map
    #map_start_pose: [0.0, 0.0, 0.0]
    map_start_at_dock: true

    # ============================================================
    # Timing & Transform Parameters
    # ============================================================
    debug_logging: false
    throttle_scans: 1                      # process every scan
    transform_publish_period: 0.02         # smoother TF publishing
    map_update_interval: 5.0               # balanced refresh rate
    resolution: 0.05                       # typical for 2D lidars
    min_laser_range: 0.2                   # avoid invalid lidar hits
    max_laser_range: 15.0                  # moderate for indoor robots
    minimum_time_interval: 0.5             # responsive but stable
    transform_timeout: 0.2
    tf_buffer_duration: 30.0               # enough for steady TFs
    stack_size_to_use: 40000000

    # ============================================================
    # Scan Matching / Loop Closure
    # ============================================================
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.5           # good responsiveness
    minimum_travel_heading: 0.5
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # ============================================================
    # Correlation Parameters
    # ============================================================
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # ============================================================
    # Loop Closure Parameters
    # ============================================================
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # ============================================================
    # Scan Matcher Parameters
    # ============================================================
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.0349       # balance between accuracy and speed
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
    min_pass_through: 2
    occupancy_threshold: 0.25

    # ============================================================
    # Lifelong Mapping Parameters (kept but moderate)
    # ============================================================
    lifelong_search_use_tree: false
    lifelong_minimum_score: 0.1
    lifelong_iou_match: 0.85
    lifelong_node_removal_score: 0.04
    lifelong_overlap_score_scale: 0.06
    lifelong_constraint_multiplier: 0.08
    lifelong_nearby_penalty: 0.001
    lifelong_candidates_scale: 0.03
